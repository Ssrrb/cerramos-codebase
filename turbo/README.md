# Turbo Scaffolding

`turbo/` holds repo-wide generator and scaffolding support that complements `turbo.json`.

Keep task orchestration in `turbo.json`.
Keep generator templates and monorepo scaffolding support in this folder.

Do not mix product feature logic into workspace scaffolding.
