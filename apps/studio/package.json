{
  "name": "studio",
  "version": "0.0.0",
  "scripts": {
    "dev": "if [ -z \"$DATABASE_URL\" ]; then echo '[studio] DATABASE_URL not set; skipping Prisma Studio.'; tail -f /dev/null; else prisma studio --config ../../packages/database/prisma.config.ts --port 3005; fi",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  },
  "devDependencies": {
    "prisma": "7.5.0"
  }
}
