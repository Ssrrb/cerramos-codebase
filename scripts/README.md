# Repo Scripts

`scripts/` holds root-level automation and helper scripts for operating the repo.

Keep only repo-wide workflow helpers here.
Do not put app-specific scripts or reusable product logic in this folder.

If a script becomes a stable capability of one app or package, move it to that owner.
